# GitHub Pages source

This `docs/` folder is the single source of truth for the browser prototype that GitHub Pages deploys.

Use either Pages configuration:

- **GitHub Actions** with `.github/workflows/deploy-web-prototype.yml` (publishes `docs/`)
- **Deploy from a branch** with folder `/docs`
